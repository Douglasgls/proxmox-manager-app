# API URLs (Arquitetura Single Server)
# Em produção, quando servido pelo próprio Agent, VITE_API_URL deve ser /api e VITE_WS_URL deve ficar vazio (automático).
# Para desenvolvimento local (separado), mude para http://localhost:8000/api e ws://localhost:8000/ws
VITE_API_URL=/api
VITE_WS_URL=
